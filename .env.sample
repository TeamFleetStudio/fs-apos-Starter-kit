# --------------------------------------------------
# App / Server
# --------------------------------------------------
NODE_ENV=development

# Base URL used by Apostrophe for absolute URLs
APOS_BASE_URL="http://localhost:3002"

# Port the app should try to run on
APOS_BASE_PORT=3002

# Optional: override runtime port
# PORT=3002

# Optional: Apostrophe short name (defaults in code if omitted)
APOS_SHORT_NAME=a3-starter-kit-marketing


# --------------------------------------------------
# MongoDB
# IMPORTANT:
# - Always include the database name in the URI path
# - Keep authSource=admin if the user was created in admin
# --------------------------------------------------
APOS_MONGODB_URI="mongodb://mongo:YOUR_PASSWORD@fshackathon.fsgarage.in:35435/a3-starter-kit-marketing?authSource=admin&tls=false"

